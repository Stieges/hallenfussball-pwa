# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# MCP Server und sensitive Daten
mcp-ai-hub-analyzer/
.mcp.json
.claude/
**/mcp*.md
**/MCP*.md

# CLAUDE.md ist lokal in .claude/CLAUDE.md konsolidiert (gitignored).
# Root-Level CLAUDE.md soll NICHT versehentlich getrackt werden.
/CLAUDE.md

# API Keys und Credentials
*.key
*.pem
.env
.env.local
.env.*.local
**/credentials.json
**/*secret*
**/*apikey*
TEST-CREDENTIALS.md

# Entwickler-Skripte mit API Keys (AI Hub)
evaluate-plan.js
clarify-*.js
search-property-names.js
validate-bye-fix.js

# Interne Dokumentation, Pläne, Reviews, Investigates (nicht öffentlich)
docs/
reviews/

# Playwright test results
test-results/
test-reports/
playwright-report/
# Playwright cloud-setup storageStates (echte, wenn auch nur lokale Test-Sessions) — Task T3
playwright/.auth/

# Vercel
.vercel/
supabase/.temp/
scripts/dev-mcp-server.ts

# Local-only helper scripts (user environment, not part of the project)
/loc_refiner.py
/run-individual-tests.sh

# Backup files and CI/build run outputs
*.bak.*
ci_logs.txt
.vercel
.env*.local

# Local diagnostic artifacts (HAR analyses, screenshots from chrome-devtools MCP)
.tmp-repro/

# Vitest coverage output (M0)
coverage/
