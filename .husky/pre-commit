# Check for config file changes that need CI sync
if git diff --cached --name-only | grep -qE "playwright\.config\.ts|vite\.config\.ts|\.github/workflows/ci\.yml"; then
  echo ""
  echo "⚠️  CI-RELEVANTE DATEIEN GEÄNDERT!"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""

  # Show which files changed
  if git diff --cached --name-only | grep -q "playwright.config.ts"; then
    echo "📋 playwright.config.ts geändert"
    echo "   → Prüfe: Stimmen Projekt-Namen mit ci.yml überein?"
    echo "   → Prüfe: Stimmt der Port mit vite.config.ts überein?"
  fi

  if git diff --cached --name-only | grep -q "vite.config.ts"; then
    echo "⚙️  vite.config.ts geändert"
    echo "   → Prüfe: Stimmt server.port mit playwright.config.ts überein?"
  fi

  if git diff --cached --name-only | grep -q "ci.yml"; then
    echo "🔄 ci.yml geändert"
    echo "   → Prüfe: Stimmen matrix.project Namen mit playwright.config.ts überein?"
  fi

  echo ""
  echo "Aktuelle Ports:"
  echo "   vite.config.ts:       $(grep -E 'port:\s*[0-9]+' vite.config.ts 2>/dev/null | head -1 | grep -oE '[0-9]+')"
  echo "   playwright.config.ts: $(grep -E "PORT.*'[0-9]+'" playwright.config.ts 2>/dev/null | grep -oE '[0-9]+')"
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
fi

# Check for design token changes that need sync verification
if git diff --cached --name-only | grep -qE "src/design-tokens/|src/styles/global\.css"; then
  echo ""
  echo "🎨 DESIGN TOKEN DATEIEN GEÄNDERT"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Prüfe Token-Synchronisation..."
  if ! npm run tokens:check; then
    echo ""
    echo "❌ Token Sync Check fehlgeschlagen!"
    echo "   Stelle sicher, dass alle Dateien synchron sind:"
    echo "   - semantic*.ts (gleiche Properties)"
    echo "   - cssVars.ts (alle Properties gemappt)"
    echo "   - global.css (alle CSS Variables in :root und [data-theme])"
    echo ""
    exit 1
  fi
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
fi

# Check for terminology changes that need sync verification
if git diff --cached --name-only | grep -qE "src/i18n/"; then
  echo ""
  echo "🈯 TERMINOLOGIE-DATEIEN GEÄNDERT"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Prüfe Terminologie-Konsistenz..."
  if ! npm run terms:check; then
    echo ""
    echo "❌ Terminology Check fehlgeschlagen!"
    echo "   Stelle sicher, dass:"
    echo "   - alle Sprachdateien dieselben Schlüssel haben (Schlüssel-Parität)"
    echo "   - kein verbotenes Synonym aus glossary.json in einem Locale-Wert steht"
    echo "   - jeder Glossar-Eintrag seinen Schlüssel in sport.json hat"
    echo ""
    exit 1
  fi
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
fi

npx lint-staged
npm run test -- --run
